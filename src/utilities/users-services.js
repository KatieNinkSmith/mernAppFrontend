// I need to pass in userData because this is attempting to add a new user to the database
export async function signUp(userData) {
  return {
    name: "Your mom",
    email: "email@yourmom.com",
  };
}
